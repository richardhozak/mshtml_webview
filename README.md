Rust POC of embedding mshtml WebBrowser COM component into window.

Uses `com-rs` and `winapi`. If you want full-fledged webview, look at [web-view](https://github.com/Boscop/web-view).

![preview](preview.png "Preview")